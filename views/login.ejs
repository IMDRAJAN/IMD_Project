<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel='stylesheet' href='/css/login.css' />

    <title>login</title>
</head>
<body>
    
   <form  method="post">
    <table id="Tabel">

        <tr>
            <td>
                <h1> Login </h1>
            </td>

        </tr>

        <tr>
            <td>
                <div>
                    <label for="email"> Email Id</label>
                </div>
                <div>
                    <input id="email" type="email"  name="Email" placeholder="enter email" required>
                    <% if (typeof notFindMail !=='undefined' && notFindMail) { %>
                       <small> mailId is not found </small> 
                    <% } else { %>
                    <% } %>
                </div>
            </td>

        </tr>

        <tr>
            <td>
                <div>
                    <label for="password" >Password</label>
                </div>
                <div>
                    <input type="password"   name="Password" placeholder="enter Password" required>
                    <% if (typeof wrongPassword !=='undefined' && wrongPassword) { %>
                        <small> wrong password </small> 
                     <% } else { %>
                     <% } %>
                </div>
                <span><a href="/verification">forget password</a></span>
            </td>
        </tr>


        <tr>
            <td>
                <div>
                    <input type="submit"  value="Login"> 
                </div>   
            </td>
        </tr>

        <tr>
            <td>
                <div>
                    <button> <a href="/registration"> Sin in </a> </button>
                </div>
            </td>
        </tr>

    </table>

</form>

</body>
</html>